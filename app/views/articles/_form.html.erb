<%= form_for @article do |f| %>

  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@article.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group col-xs-3">
    <label for="title">Title<br></label>
    <input type="text" class="form-control" name="article[title]"></input>
  </div>

  <div class="form-group col-xs-3">
    <label for="title">Text<br></label>
    <input type="text" class="form-control" name="article[text]"></input>
  </div>
  <!-- <div class="form-group">
    <button type="button" class="btn btn-primary" name="article[submit]">Submit</button>
  </div> -->

  <p>
    <%= f.submit :Submit, class: 'btn btn-primary' %>
  </p>

<% end %>
